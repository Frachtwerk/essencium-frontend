{
  "name": "@frachtwerk/essencium-frontend",
  "version": "0.3.0",
  "private": true,
  "description": "This is a React-based starter monorepo consisting of a component library, docs and a boilerplate.",
  "keywords": [
    "starter",
    "boilerplate",
    "frontend",
    "react",
    "mantine"
  ],
  "homepage": "https://github.com/Frachtwerk/essencium-frontend/blob/master/README.md",
  "bugs": {
    "url": "https://github.com/Frachtwerk/essencium-frontend/issues"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Frachtwerk/essencium-frontend.git"
  },
  "license": "MIT",
  "author": "Frachtwerk GmbH",
  "scripts": {
    "build": "NX_BRANCH=$(git rev-parse --abbrev-ref HEAD) nx build app",
    "clean": "nx run-many --target=clean && rimraf dist node_modules",
    "clean:install": "pnpm i -w && pnpm clean && pnpm install",
    "dev": "nx dev app",
    "lint": "NX_BRANCH=$(git rev-parse --abbrev-ref HEAD) nx affected --target=lint --parallel=5",
    "format": "NX_BRANCH=$(git rev-parse --abbrev-ref HEAD) nx affected --target=format --parallel=5",
    "setup-env": "pnpm install && husky install",
    "test": "NX_BRANCH=$(git rev-parse --abbrev-ref HEAD) nx affected --target=test",
    "test:watch": "NX_BRANCH=$(git rev-parse --abbrev-ref HEAD) nx run-many --target=test:watch --parallel=5",
    "test:unit": "NX_BRANCH=$(git rev-parse --abbrev-ref HEAD) nx affected --target=test:unit --parallel=5",
    "test:e2e": "NX_BRANCH=$(git rev-parse --abbrev-ref HEAD) nx affected --target=test:e2e --parallel=5",
    "sync-versions": "ts-node ./scripts/sync-versions.ts"
  },
  "dependencies": {
    "@nrwl/nx-cloud": "15.1.1"
  },
  "devDependencies": {
    "@commitlint/cli": "17.4.4",
    "@commitlint/config-conventional": "17.4.4",
    "@nrwl/eslint-plugin-nx": "15.8.6",
    "@types/fs-extra": "11.0.1",
    "@types/node": "18.14.1",
    "@typescript-eslint/parser": "5.54.0",
    "eslint": "8.35.0",
    "eslint-plugin-prettier": "4.2.1",
    "eslint-plugin-simple-import-sort": "10.0.0",
    "husky": "8.0.3",
    "nx": "15.8.6",
    "prettier": "2.8.4",
    "rimraf": "4.4.0",
    "ts-node": "10.9.1",
    "typescript": "4.9.5",
    "fs-extra": "11.1.1"
  },
  "engines": {
    "node": ">=16",
    "pnpm": "7"
  },
  "volta": {
    "node": "16.19.1"
  }
}
