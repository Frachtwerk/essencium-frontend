version: "9.4.4"
from: "9.4.0"
date: "2025-09-09"

changes:
  # Fix TailwindCSS import issues (9.4.1 through 9.4.4)
  - type: file_tracking
    description: "Fix TailwindCSS globals.css import chain: revamp globals.css to correctly layer theme, base, mantine, components, and utilities; import from essencium-lib/dist instead of src"
    files:
      - path: "src/globals.css"
        action: modified
    pr: "https://github.com/Frachtwerk/essencium-frontend/pull/888"
